# Workspace Deployment Rules

- **Production Deployments**: Never run `vercel --prod` or perform any production deployments unless the user explicitly types the phrase "deploy to production" in their prompt.
- **Preview Deployments**: Preview deployments (e.g., `vercel`) are permitted without explicit confirmation.
